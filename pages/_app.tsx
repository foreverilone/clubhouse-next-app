import '../styles/globals.scss'

function App({ Component, pageProps }) {
  return <Component {...pageProps} />
}

export default App;

// App отображает все приложение, в котором рендерятся все компоненты по определенному адресу
